% x = linspace(-0.2,0.2,41);
% N_points = size(x,2);
% motor_orientation = [0 0 0 0];
% 
% for i = 1:N_points
%     position = [0;x(i);0];
%     B(:,i) = magnetic_field(position, motor_orientation);
% end
% 
% plot(x,B(3,:));

x = linspace(-0.14,0.14,6000*14/15);
N_points = size(x,2);
motor_orientation = [0 0 0 0];

for i = 1:N_points
    position = [x(i);0;0];
    B(:,i) = magnetic_field(position, motor_orientation);
    if 
end

plot(x,B(1,:));